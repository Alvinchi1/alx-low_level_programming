#include <unistd.h>

/**
 * _putchar - main entry into code, writes char c to stdout
 * 2c: the character to print
 * Return: Success on 1
 * on error, -1 is returned and error is set appropriately
 */

int _putchar(char o)
{
	return(write(1, &c, 1);
}
